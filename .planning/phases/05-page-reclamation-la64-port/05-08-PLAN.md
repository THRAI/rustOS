---
phase: 05-page-reclamation-la64-port
plan: 08
type: execute
wave: 3
depends_on: []
files_modified:
  - tests/libc-test/Makefile
  - tests/libc-test/run.sh
  - Makefile
  - .planning/TEST-05-STATUS.md
autonomous: true
requirements: [TEST-05]

must_haves:
  truths:
    - "libc-test suite is integrated into build system"
    - "Tests run on rv64 QEMU with meaningful pass rate"
    - "Test results are logged for tracking"
  artifacts:
    - path: "tests/libc-test/Makefile"
      provides: "libc-test build integration"
      min_lines: 30
    - path: "tests/libc-test/run.sh"
      provides: "Test runner script"
      min_lines: 50
    - path: ".planning/TEST-05-STATUS.md"
      provides: "Test results tracking"
      min_lines: 20
  key_links:
    - from: "Makefile"
      to: "tests/libc-test/run.sh"
      via: "test-libc target"
      pattern: "test-libc.*run.sh"
---

<objective>
Activate libc-test suite for rv64 with build integration and result tracking.

Purpose: libc-test is a comprehensive POSIX compliance test suite (~463 tests). Activating it provides continuous validation of syscall correctness and identifies gaps in kernel implementation.

Output: libc-test integrated into Makefile, runs on rv64 QEMU, results logged in TEST-05-STATUS.md.
</objective>

<execution_context>
@/Users/3y/.claude/get-shit-done/workflows/execute-plan.md
@/Users/3y/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-page-reclamation-la64-port/05-CONTEXT.md

# Existing test infrastructure
@Makefile
@kernel/src/mm/vm/test_integration.rs
</context>

<interfaces>
<!-- Existing test patterns -->

From Makefile:
```makefile
test-rv64:
	@echo "Running QEMU integration tests..."
	$(QEMU_RV64) -kernel $(KERNEL_RV64) -nographic | grep "PASS"

test-all: test test-rv64
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Integrate libc-test build system</name>
  <files>tests/libc-test/Makefile, tests/libc-test/run.sh, Makefile</files>
  <action>
Create tests/libc-test/ directory structure:

1. Clone or download libc-test suite (https://repo.or.cz/libc-test.git)
2. Create tests/libc-test/Makefile:
   - CC = riscv64-linux-musl-gcc (cross-compiler for rv64)
   - CFLAGS = -static -O2
   - Build all test binaries in tests/libc-test/bin/
   - Target: all (builds all tests)

3. Create tests/libc-test/run.sh:
   - For each test binary in bin/:
     - Copy to ext4 test disk image
     - Boot QEMU with test binary as init
     - Capture output (PASS/FAIL)
     - Log result to TEST-05-STATUS.md
   - Summary: X/463 tests passed

4. Update root Makefile:
   - Add test-libc target: cd tests/libc-test && ./run.sh
   - Add test-libc to test-all dependency

Note: Initial pass rate will be low (expect 10-20%). Goal is infrastructure, not full pass rate yet.
  </action>
  <verify>
    <automated>make test-libc 2>&1 | grep -q "tests passed" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>libc-test builds, run.sh executes tests, results logged</done>
</task>

<task type="auto">
  <name>Task 2: Create TEST-05-STATUS.md tracking document</name>
  <files>.planning/TEST-05-STATUS.md</files>
  <action>
Create .planning/TEST-05-STATUS.md with structure:

```markdown
# TEST-05: libc-test Status

**Activated:** 2026-03-08
**Total tests:** 463
**Current pass rate:** X/463 (Y%)

## Test Categories

| Category | Total | Pass | Fail | Skip |
|----------|-------|------|------|------|
| string   | 45    | ?    | ?    | ?    |
| math     | 120   | ?    | ?    | ?    |
| stdio    | 38    | ?    | ?    | ?    |
| ...      | ...   | ...  | ...  | ...  |

## Known Failures

- test_name: reason (missing syscall, incorrect behavior, etc.)

## Next Steps

- Fix high-impact failures (syscalls used by many tests)
- Investigate math library issues
- Add missing POSIX features
```

Populate with initial run results from Task 1.
  </action>
  <verify>
    <automated>test -f .planning/TEST-05-STATUS.md && grep -q "Current pass rate" .planning/TEST-05-STATUS.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>TEST-05-STATUS.md exists with initial test results</done>
</task>

</tasks>

<verification>
- libc-test suite builds for rv64
- run.sh executes tests on QEMU
- TEST-05-STATUS.md exists with results
- make test-libc target works
</verification>

<success_criteria>
- TEST-05 requirement satisfied: libc-test suite active
- Build integration complete (Makefile + run.sh)
- Results tracking in place (TEST-05-STATUS.md)
- Baseline pass rate established (even if low)
</success_criteria>

<output>
After completion, create `.planning/phases/05-page-reclamation-la64-port/05-08-SUMMARY.md`
</output>
